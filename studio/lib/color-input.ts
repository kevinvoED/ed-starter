import { COLOR_OPTIONS } from "@/lib/consts";

export const simplerColorInputOptions = {
  defaultColorFormat: "rgba" as const,
  defaultColorList: COLOR_OPTIONS,
  enableSearch: true,
  showColorValue: true,
};
