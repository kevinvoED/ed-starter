import { DocumentIcon } from "@sanity/icons";
import { createPageType } from "@/schemas/pageBuilder";

export default createPageType({
  name: "solutions-child",
  title: "Solutions Page",
  icon: DocumentIcon,
});
